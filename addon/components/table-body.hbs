<tbody class="ui-table__body">
  {{#each this.processedRows as |rowData idx|}}
    <Row 
      @validationFunction={{@validationFunction}}
      @selectedItems={{@selectedItems}} 
      @rowData={{rowData}} 
      @onClick={{@handleRowClick}} 
      @idx={{idx}}
    />
  {{/each}}
</tbody>